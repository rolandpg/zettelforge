apiVersion: v1
kind: Service
metadata:
  name: threatrecall-api
  labels:
    app: threatrecall-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: threatrecall-api
