apiVersion: v1
kind: ServiceMonitor
metadata:
  name: threatrecall-api
  labels:
    app: threatrecall-api
    release: prometheus
spec:
  selector:
    matchLabels:
      app: threatrecall-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
