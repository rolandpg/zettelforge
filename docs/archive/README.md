# Archive

Historical documents that no longer reflect the current release but are
preserved so prior claims can be audited.

| File | Description | Last applicable version |
|------|-------------|-------------------------|
| `SKILL.md` | Claude Code skill manifest for an earlier alpha packaging experiment. Superseded by the MCP server (`python -m zettelforge.mcp`) and the `skills/` directory. | 1.0.0-alpha.1 |
| `PACKAGE_SUMMARY.md` | Early packaging summary produced during the initial PyPI release. Superseded by `README.md`, `CHANGELOG.md`, and the published metadata in `pyproject.toml`. | 1.0.0-alpha.1 |

These files are not included in the rendered documentation site.
