

## Session Update — Sat 21 Mar 2026 16:27 CDT

### CTI Platform Fixes

**ATT&CK Techniques page (HTTP 500)**
- Root cause: Django template can't call `.split(',')` with args — needs custom filter
- Fix: Created `/home/rolandpg/cti-workspace/intel/templatetags/cti_filters.py` with `split` and `trim` filters
- Updated `techniques.html` to use `{% load cti_filters %}` and `|split:','` and `|trim`
- Server restarted, all 6 pages now return 200

**OTX cron fix**
- OTX_API_KEY wasn't loading in cron because `export` was missing
- Fixed: wrapped in `bash -c "export OTX_API_KEY=..."` instead of inline env var
- Was working inline (manual run) but cron needs explicit bash wrapper

### UX QA Results (subagent ran)

Full report: `memory/cti-ux-qa-report.md`

Critical (fixed):
- ATT&CK Techniques page 500 — template syntax error — FIXED

Medium priority (not yet fixed):
- No loading states on data-heavy pages
- No user-friendly error pages
- No keyboard navigation

Low priority:
- Export functionality (CSV/PDF)
- Tooltips on technical fields
- Responsive design improvements

All interactions passed: filters, sort, navigation, search.

### Collection Run — Sat 21 Mar 1400 CDT

| Source | Total | DIB |
|--------|-------|-----|
| NVD CVE | 3 | 3 |
| CISA KEV | 5 | 5 |
| THN | 50 | 24 |
| CSHub | 96 | 43 |
| CybersecDive | 10 | 8 |
| DataBreaches | 5 | 0 |
| IC3 CSA | 2 | 2 |
| OTX | 10 pulses, 71 IOCs | — |

15 threat alerts generated (5 KEV + 10 High EPSS).

### Threat Intel Program Assessment — Delivered to Patrick

For advisory + social media mission, key gaps:

1. **No confidence language in posts** — posts don't cite source reliability (A-F per FIRST CTI-SIG)
2. **No ATT&CK TTP context in posts** — engagement is higher when posts map to techniques
3. **No feedback loop** — not tracking what audience engages with
4. **No detection content** — Sigma/YARA rules would 10x post value
5. **No strategic CTI** — all tactical, no leadership briefings

Built and deployed: CTI metadata framework (confidence, source reliability, calibrated language, ATT&CK mapping) integrated into `threat_alert.py` output.

### Bot Status
- CTI platform: http://192.168.1.70:8000/intel/
- All pages: 200 OK
- OTX cron: fixed
- 17 alerts in queue for Patrick review
