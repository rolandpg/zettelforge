# 2026-04-05 — System Status & Honcho Issue

## Gateway Status
✅ OpenClaw Gateway: Running (pid 515792, port 18789)
✅ RPC Probe: OK

## Memory System (A-MEM)
✅ MemoryStore: 496 notes stored
✅ EntityIndexer: 5 entities indexed
✅ MemoryManager: Operational
✅ Daily Timer: Active (next: 06:00 CDT)
✅ Weekly Timer: Active (next: Fri 23:00 CDT)

## Honcho Issue
❌ **STATUS: NOT RUNNING**

### Root Cause
1. Systemd service file had invalid `%I` template variable
2. Docker image `ghcr.io/plasticlabs/honcho:latest` denied (auth required)
3. PostgreSQL container starts but stops immediately (healthcheck/docker issue)
4. Direct Python run requires extensive API key configuration:
   - Google API key (for summarization)
   - OpenAI API key (for embeddings)
   - Anthropic API key (for dialectic/deriver)

### Config Problem
OpenClaw config pointed Honcho to `localhost:8000` — that's the Django CTI app. Changed to `localhost:8001`.

### Options

**Option A: A-MEM Only (Recommended for now)**
- Disable Honcho plugin in openclaw.json
- Rely on A-MEM (496 notes, working perfectly)
- Fastest path to operational

**Option B: Full Honcho Server**
- Set up PostgreSQL on port 5433 (container keeps stopping)
- OR use SQLite backend
- Configure all API keys in .env
- Run Honcho server on port 8001
- More features (conclusions, dialectic, deriver) but requires cloud API keys

## Timers Active
| Timer | Status | Next Run |
|-------|--------|----------|
| openclaw-memory-daily | Active | Sun 06:00 CDT (3h 49min) |
| openclaw-fleet-sync | Active | Sun 07:00 CDT (4h 49min) |
| openclaw-nexus | Active | Just ran (02:00 CDT) |
| openclaw-cti-cfr | Active | Sun 07:02 CDT (4h 52min) |
| openclaw-memory-weekly | Active | Fri 23:00 CDT (5 days) |

## Fleet Status
- SignalDeck: Running (port 8080)
- Vault: Running (port 8200)
- CTI Stack: Partial (some containers restarting)

## Recommendation
Disable Honcho plugin for now. A-MEM provides sufficient memory functionality without external API dependencies. Honcho adds features (conclusions, dialectic) but requires cloud API keys and more infrastructure.

---
**Decision needed:** Disable Honcho plugin or configure full server?

## ThreatRecall API Cleanup — 22:14 CDT
✅ **Removed duplicate:** `memory/api/` directory deleted
- Duplicate stub implementation (single file with TODOs)
- Real implementation lives in `threatrecall-api/` (production-ready, 12/13 tests passing)
- Avoids confusion, consolidates to single source of truth

## ThreatRecall API Testing — 22:15-22:25 CDT

### Bug Fixes
1. **MemoryManager/VectorRetriever isolation bug** — Fixed `vector_retriever.py` to accept optional `store` parameter; updated `memory_manager.py` to pass its store instance. This ensures recall searches the correct tenant-isolated storage instead of default paths.

2. **Error response structure** — Fixed `get_note` endpoint in `memory.py` to wrap errors in proper GOV-005 envelope: `{"detail": {"error": {"code": ..., "message": ..., "request_id": ...}}}`

### Test Results
| Suite | Status |
|-------|--------|
| Unit tests | 13/13 passing ✅ |
| Integration tests | 9/15 passing ⚠️ |

**Passing:** Health endpoints, tenant creation, auth, remember/recall roundtrip, error handling

**Known issues:**
- Duplicate tenant detection not implemented (returns 201 instead of 409)
- Key rotation doesn't invalidate old keys (no grace period enforcement)
- Entity extraction confidence varies (test expects specific entities)
- Note ID lookup is linear scan (MVP limitation documented)
- List pagination cursor implementation is offset-based, not semantic

### Run Instructions
```bash
cd threatrecall-api
export TR_DATA_DIR=/tmp/threatrecall-test
export TR_SECRETS_BACKEND=env
export PYTHONPATH=/home/rolandpg/.openclaw/workspace/memory:src

# Unit tests
python3 -m pytest tests/unit/test_api.py -v

# Integration tests
python3 -m pytest tests/integration/test_integration.py -v
```

### Production Readiness
- Core API is functional and tested
- Known limitations are documented in DEVELOPMENT_STATUS.md
- MVP ready for containerization and deployment

### Edge Cases to Address Later (23:06 CDT)
**4 remaining failing integration tests:**
1. `test_remember_deduplication` - MemoryManager not detecting duplicates
2. `test_recall_with_entity_extraction` - Entity extraction confidence inconsistent
3. `test_get_note_by_id` - Note lookup by ID failing  
4. `test_list_notes_pagination` - Pagination logic issues

**Decision:** Defer these for post-MVP. Core functionality is solid.
